pipeline {
  agent any
    stages {
        stage('build version to package.json') {
            steps {
		      sh '''
			    cat package.json
			'''
                }
            }
         }
      }
